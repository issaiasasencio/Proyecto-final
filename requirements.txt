# === FLEX-SORT: DEPENDENCIAS DEL SISTEMA MLOPS ===

# --- Interfaz y Gráficos ---
customtkinter==5.2.2    # Framework de la interfaz moderna
Pillow==12.1.1          # Procesamiento de imágenes para la UI
darkdetect==0.8.0       # Detección automática de tema (Dark/Light)

# --- Inteligencia Artificial y Visión ---
ultralytics==8.4.37     # Framework YOLO AI (Detección y Entrenamiento)
opencv-python==4.13.0.92 # Visión computacional en tiempo real
numpy>=1.24.0           # Operaciones numéricas base
matplotlib==3.10.8      # Generación de reportes y gráficas de IA
torch==2.7.1+cu118      # Backend de modelos (Optimizado para CUDA cu118)
torchvision==0.22.1+cu118
torchaudio==2.7.1+cu118

# --- Comunicación y Despliegue (Edge) ---
paramiko==4.0.0         # Protocolo SSH para comunicación con Raspberry Pi
scp==0.15.0             # Transferencia de modelos vía SCP
psutil==7.2.2           # Monitoreo de hardware (CPU/RAM/GPU)

# --- Utilidades de Datos ---
PyYAML==6.0.3           # Manejo de datasets (data.yaml) y configuración
tqdm==4.67.3            # Barras de progreso en consola
packaging==26.0         # Manejo de versiones internas

# --- Backend de Optimización (Inferencia Edge) ---
onnx==1.21.0            # Exportación de modelos a formato abierto
ncnn==1.0.20260114      # Acelerador de inferencia para procesadores ARM
pnnx==20260409          # Conversor de modelos PyTorch a NCNN

# === NOTAS DE INSTALACIÓN ===
# Si tienes una GPU NVIDIA RTX 2060 y necesitas soporte CUDA, utiliza:
# pip3 install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
